/**
 * App.tsx
 * ---------------------------------------------------------------------------
 * Thin shell. All route definitions live in routes/AppRoutes.tsx so this file
 * stays stable as the app grows.
 */
import AppRoutes from './routes/AppRoutes';

export default function App() {
  return <AppRoutes />;
}
